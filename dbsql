-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


ALTER TABLE IF EXISTS options.ticker DROP CONSTRAINT IF EXISTS stocks_stock_id_fkey;

ALTER TABLE IF EXISTS options.candle_stick DROP CONSTRAINT IF EXISTS ticker_symbol_token_fkey;



DROP TABLE IF EXISTS options.ticker;

CREATE TABLE IF NOT EXISTS options.ticker
(
    symbol_token integer NOT NULL,
    stock_id integer,
    expiry_date date,
    strike_price numeric,
    option_type character varying,
    PRIMARY KEY (symbol_token)
);

DROP TABLE IF EXISTS options.candle_stick;

CREATE TABLE IF NOT EXISTS options.candle_stick
(
    time_stamp date,
    symbol_token integer,
    open integer,
    high integer,
    low integer,
    close integer,
    volume bigint,
    open_interest integer,
    PRIMARY KEY (time_stamp)
);

DROP TABLE IF EXISTS options.stock;

CREATE TABLE IF NOT EXISTS options.stock
(
    stock_id integer NOT NULL GENERATED ALWAYS AS IDENTITY ( INCREMENT 1 ),
    name character varying,
    nifty_fifty_index integer,
    PRIMARY KEY (stock_id)
);

ALTER TABLE IF EXISTS options.ticker
    ADD CONSTRAINT stocks_stock_id_fkey FOREIGN KEY (stock_id)
    REFERENCES options.stock (stock_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS options.candle_stick
    ADD CONSTRAINT ticker_symbol_token_fkey FOREIGN KEY (symbol_token)
    REFERENCES options.ticker (symbol_token) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;

END;