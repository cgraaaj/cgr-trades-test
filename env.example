# .env.example
# Copy this file to .env and fill in your actual values

# Database connection string (example for PostgreSQL)
DATABASE_URL=(
    "postgresql+psycopg2://sd_admin:%s@192.168.1.72:5430/stock-dumps"
    % quote("sdadmin@postgres")
)

# Alternative DB connection string (if needed)
DB_CONNECTION_STRING=(
    "postgresql+psycopg2://sd_admin:%s@192.168.1.72:5430/stock-dumps"
    % quote("sdadmin@postgres")
)

# API endpoint for stock service
STOCK_SVC_URL=http://192.168.1.72:1234