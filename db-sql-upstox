-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


ALTER TABLE IF EXISTS options.instrument DROP CONSTRAINT IF EXISTS stock_id_fkey;



DROP TABLE IF EXISTS options.instrument;

CREATE TABLE IF NOT EXISTS options.instrument
(
    id uuid NOT NULL,
    stock_id uuid,
    segment character varying,
    name character varying,
    exchange character varying,
    expiry date,
    expiry_epoch bigint,
    instrument_type character varying,
    asset_symbol character varying,
    underlying_symbol character varying,
    instrument_key character varying,
    lot_size integer,
    freeze_quantity numeric,
    exchange_token character varying,
    minimum_lot integer,
    asset_key character varying,
    underlying_key character varying,
    tick_size numeric,
    asset_type character varying,
    underlying_type character varying,
    trading_symbol character varying,
    strike_price numeric,
    PRIMARY KEY (id)
);

DROP TABLE IF EXISTS options.ticker;

CREATE TABLE IF NOT EXISTS options.ticker
(
    id uuid NOT NULL,
    instrument_id uuid,
    time_stamp timestamp without time zone,
    open integer,
    high integer,
    low integer,
    close integer,
    volume bigint,
    open_interest bigint,
    PRIMARY KEY (id)
);

DROP TABLE IF EXISTS options.stock;

CREATE TABLE IF NOT EXISTS options.stock
(
    id uuid NOT NULL,
    name character varying,
    nifty_fifty_index integer,
    PRIMARY KEY (id)
);

ALTER TABLE IF EXISTS options.instrument
    ADD CONSTRAINT stock_id_fkey FOREIGN KEY (stock_id)
    REFERENCES options.stock (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;

END;